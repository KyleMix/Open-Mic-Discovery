# TEMPORARY DIAGNOSTIC (remove once the e2e content failure is understood).
#
# Third possibility: a guest cold start is being routed to the auth stack in
# the release build instead of browsing Discover. If THIS flow passes, the
# app landed on the sign-in screen (title "Sign in"), which would point at
# the AuthGate routing rather than the loading gate or an empty feed.
appId: com.openmicexplorer.app
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: 'Sign in'
    timeout: 12000
