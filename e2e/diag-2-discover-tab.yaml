# TEMPORARY DIAGNOSTIC (remove once the e2e content failure is understood).
#
# Counterpart to diag-1. If THIS flow passes, the tabs DID render and the
# real flows failed because the Discover feed was empty (the seeded-data /
# home-area theory), not because the app was stuck before the tabs. The tab
# label is "Discover" (src/app/(tabs)/_layout.tsx). Exactly one of diag-1 or
# diag-2 passing tells us which layer to fix; if neither passes, the app is
# stuck on the native splash / a JS crash with no rendered text at all.
appId: com.openmicexplorer.app
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: 'Discover'
    timeout: 12000
