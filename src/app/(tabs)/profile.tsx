import { PhaseShell } from '@/components/phase-shell';

export default function ProfileScreen() {
  return (
    <PhaseShell title="Profile" body="Sign in, roles, and settings arrive with auth in Phase 1." />
  );
}
