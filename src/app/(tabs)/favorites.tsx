import { PhaseShell } from '@/components/phase-shell';

export default function FavoritesScreen() {
  return (
    <PhaseShell
      title="Favorites"
      body="Mics you save will live here, with reminders before each night."
    />
  );
}
