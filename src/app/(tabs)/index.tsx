import { PhaseShell } from '@/components/phase-shell';

export default function DiscoverScreen() {
  return (
    <PhaseShell
      title="Discover"
      body="Find open mics for music, comedy, and poetry near you. The map and list arrive in Phase 2."
    />
  );
}
